<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Caméra pour le scan code-barres -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!--
      Pas de permission de stockage : import comme export passent par le Storage Access
      Framework (ACTION_OPEN_DOCUMENT / ACTION_CREATE_DOCUMENT). L'utilisateur choisit le
      fichier lui-même et le système délègue l'accès à cet URI précis — READ_EXTERNAL_STORAGE
      et READ_MEDIA_IMAGES étaient demandées pour rien, et READ_MEDIA_IMAGES ne couvre de
      toute façon que les images, jamais un CSV.
    -->

    <!-- Vibration pour retour haptique scan -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      Synchro WiFi vers l'API Nirgescom, sur le LAN du magasin. L'app reste utilisable entièrement
      hors ligne : la collecte, l'import et l'export CSV ne dépendent d'aucun réseau, et la synchro
      est un canal supplémentaire, pas un remplacement.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      allowBackup="false" : la sauvegarde automatique Android copie la base Room telle quelle et
      peut la restaurer sur une autre tablette. Deux magasiniers se retrouveraient avec les mêmes
      id_session, donc les mêmes hash de lignes côté serveur — la synchro de l'un écraserait
      silencieusement celle de l'autre. Les données de collecte sont censées partir par l'export
      CSV et la synchro WiFi, pas par un canal de sauvegarde invisible.
    -->
    <application
        android:name=".StockCollectApp"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StockCollect"
        android:hardwareAccelerated="true"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
